<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="?attr/actionBarSize"
    android:background="@color/colorPrimaryDark">

    <ImageButton
        android:layout_width="30dp"
        android:layout_height="30dp"
        android:background="@drawable/back"
        android:layout_centerVertical="true"
        android:layout_margin="@dimen/big_corner"
        android:id="@+id/search_imageButton_back"/>

    <EditText
        android:id="@+id/title_edittext_search"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_centerVertical="true"
        android:layout_marginBottom="14dp"
        android:layout_marginRight="30dp"
        android:layout_marginLeft="6dp"
        android:layout_marginTop="10dp"
        android:layout_toEndOf="@+id/search_imageButton_back"
        android:background="@drawable/shape_search"
        android:hint="搜索"
        android:paddingLeft="@dimen/big_spacing"
        android:textColor="@color/accent_blue"
        android:textColorHint="@color/accent_gray"
        android:textSize="@dimen/sub_big_text"/>

</RelativeLayout>